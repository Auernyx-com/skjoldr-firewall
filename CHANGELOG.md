# Changelog

## [Unreleased] - 2026-01-06

### Fixed
- Removed invalid/empty function openers for `Set-SkjoldrConservativeMode` and `Set-SkjoldrFortressMode` at the top of SkjoldrFirewall.ps1.
- Restored robust, standards-compliant implementations for both `Set-SkjoldrConservativeMode` and `Set-SkjoldrFortressMode`.
- Ensured all function blocks are properly closed and file is syntactically valid.
- All functions now use approved PowerShell verbs and follow best practices.
- File is ready for final verification and Git push.
